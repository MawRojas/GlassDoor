import { AwardsPipePipe } from './awards-pipe.pipe';

describe('AwardsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AwardsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
