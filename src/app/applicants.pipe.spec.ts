import { ApplicantsPipe } from './applicants.pipe';

describe('ApplicantsPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicantsPipe();
    expect(pipe).toBeTruthy();
  });
});
